type Voter {
    authId: String!
    district: District
}

extend type Query {
    meAsVoter: Voter
    voter(id: ID!): Voter
}

extend type Mutation {
    updateVoter(authId: String!, district: String): Voter!
}
