apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "voting-service.fullname" . }}-test-connection"
  labels:
{{ include "voting-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "voting-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
