apiVersion: v1
kind: Service
metadata:
  name: {{ include "canvote-chart.fullname" . }}
  labels:
    {{- include "canvote-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "canvote-chart.selectorLabels" . | nindent 4 }}
