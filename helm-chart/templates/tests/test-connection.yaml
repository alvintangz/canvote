apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "canvote-chart.fullname" . }}-test-connection"
  labels:
{{ include "canvote-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "canvote-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
