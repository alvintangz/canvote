{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "canvote-chart.serviceAccountName" . }}
  labels:
{{ include "canvote-chart.labels" . | nindent 4 }}
{{- end -}}
